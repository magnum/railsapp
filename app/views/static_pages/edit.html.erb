<div class="mx-auto w-full">
  <div class="mb-6 flex items-center justify-between">
    <h1 class="text-2xl font-semibold text-gray-900"><%= t("static_pages.edit_title", title: @static_page.title) %></h1>
    <%= link_to t("static_pages.view"), @static_page.public_path, class: "text-sm font-medium text-gray-600 hover:text-gray-900 hover:underline" %>
  </div>

  <%= render "form", static_page: @static_page %>
</div>
